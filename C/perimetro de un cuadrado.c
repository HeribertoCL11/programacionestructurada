// perimetro
#include <stdio.h>

int main() {
    float a;
    float total;
     printf("lado del cuadrado:");
    scanf("%f",&a);
   total=4*a;
    printf("el perimetro del cuadrado es =  %.2f\n",total);

    return 0;
}